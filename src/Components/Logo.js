import React from "react";

const Logo = () =>{
    return(
        <div style={{margin:'50px',display: 'flex', justifyContent:'flex-start'}}>
            <p>Logo</p>
        </div>
    );
}

export default Logo;